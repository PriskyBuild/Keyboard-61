// MIT License — Piano Learning App
// Entry route. The actual piano UI is rendered by the <AppShell> client component.
import AppShell from "@/components/AppShell";

export default function Home() {
  return <AppShell />;
}
